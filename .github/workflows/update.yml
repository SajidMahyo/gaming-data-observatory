name: Hourly Update and Deploy

on:
  schedule:
    # Run every hour
    - cron: '0 * * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install UV
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install Python dependencies
        run: uv sync --all-extras

      # ========================================================================
      # DISCOVER NEW GAMES (once per day at 8 AM UTC)
      # ========================================================================

      - name: Discover new games (daily at 8 AM UTC)
        run: |
          HOUR=$(date -u +%H)
          if [ "$HOUR" = "08" ]; then
            echo "🔍 Discovering new games from multiple sources..."
            uv run python -m python.main discover \
              --top-limit 200 \
              --trending-limit 100
            echo "✅ Game discovery complete"
          else
            echo "⏭️  Skipping discovery (runs only at 8 AM UTC, current hour: $HOUR)"
          fi

      # ========================================================================
      # ENRICH METADATA WITH IGDB (once per day at 8:30 AM UTC)
      # ========================================================================

      - name: Enrich game metadata from IGDB (daily at 8:30 AM UTC)
        env:
          IGDB_CLIENT_ID: ${{ secrets.IGDB_CLIENT_ID }}
          IGDB_CLIENT_SECRET: ${{ secrets.IGDB_CLIENT_SECRET }}
        run: |
          HOUR=$(date -u +%H)
          MINUTE=$(date -u +%M)
          if [ "$HOUR" = "08" ] && [ "$MINUTE" -ge "30" ]; then
            echo "🎯 Enriching game metadata from IGDB..."
            uv run python -m python.main metadata
            echo "✅ Metadata enrichment complete"
          else
            echo "⏭️  Skipping metadata enrichment (runs only at 8:30 AM UTC)"
          fi

      # ========================================================================
      # COLLECT DATA FROM MULTIPLE SOURCES (PARALLEL)
      # ========================================================================

      - name: Collect data from Steam and Twitch (parallel)
        env:
          TWITCH_CLIENT_ID: ${{ secrets.TWITCH_CLIENT_ID }}
          TWITCH_CLIENT_SECRET: ${{ secrets.TWITCH_CLIENT_SECRET }}
        run: |
          echo "🚀 Starting parallel data collection..."

          # Start Steam collection in background
          echo "🎮 Starting Steam collection..."
          uv run python -m python.main collect steam &
          STEAM_PID=$!
          echo "Steam collection started (PID: $STEAM_PID)"

          # Start Twitch collection in background
          echo "📺 Starting Twitch collection..."
          uv run python -m python.main collect twitch &
          TWITCH_PID=$!
          echo "Twitch collection started (PID: $TWITCH_PID)"

          # Wait for both to complete
          echo "⏳ Waiting for Steam collection..."
          wait $STEAM_PID
          STEAM_EXIT=$?
          echo "✅ Steam collection complete (exit code: $STEAM_EXIT)"

          echo "⏳ Waiting for Twitch collection..."
          wait $TWITCH_PID
          TWITCH_EXIT=$?
          echo "✅ Twitch collection complete (exit code: $TWITCH_EXIT)"

          # Check if both succeeded
          if [ $STEAM_EXIT -ne 0 ] || [ $TWITCH_EXIT -ne 0 ]; then
            echo "❌ One or more collections failed"
            exit 1
          fi

          echo "🎉 All data collection complete successfully"

      # ========================================================================
      # AGGREGATE KPIs (ALL SOURCES)
      # ========================================================================
      # Note: Data is already in DuckDB (inserted during collection)

      - name: Aggregate KPIs and generate rankings
        run: |
          echo "📊 Aggregating KPIs from all sources..."
          uv run python -m python.main aggregate
          echo "✅ KPIs aggregated and rankings generated"

      # ========================================================================
      # BUILD OBSERVABLE DASHBOARD
      # ========================================================================

      - name: Set up Node.js for Observable Framework
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Observable Framework
        run: npm ci

      - name: Build static site
        run: npm run build

      # ========================================================================
      # DEPLOY TO GITHUB PAGES
      # ========================================================================

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # ========================================================================
      # COMMIT DATA TO REPOSITORY
      # ========================================================================

      - name: Commit collected data
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Add new data files from all sources
          git add data/raw/steam/ data/raw/twitch/ data/duckdb/ src/data/

          if git diff --staged --quiet; then
            echo "📭 No new data to commit"
          else
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M UTC")
            git commit -m "data: hourly update ${TIMESTAMP}"
            git push
            echo "✅ Data committed and pushed"
          fi
